---
name: h2s-model-design
description: 从自然语言、草图或参考图创建和修改适合 Bambu Lab H2S 的 3D 打印模型，交付可编辑源文件、真实几何预览、可打印性检查和可切片的 Bambu Studio 工程。用于零件、收纳、外壳、摆件和多色分件设计，不负责发送任务或启动打印。
---

# H2S 自然语言建模

## 可移植性

技能目录可以整体复制，不绑定作者机器、用户名或磁盘。首次使用先读 [安装与迁移说明](INSTALL.md)。所有本文 `scripts/`、`references/` 路径相对于本 SKILL.md 所在目录，执行时解析技能根目录，不能假设当前工作目录就是技能目录。模型与报告路径由任务传入；生成的脚本同样使用脚本目录、项目相对路径或参数，不写死本机绝对路径。软件位置从 PATH、环境变量或用户本机配置解析；运行时解析出的绝对路径可以用于调用/诊断，但不能固化进可迁移配置。迁移时重建 Python 环境，不复制虚拟环境。

## 交付约定

把描述变成可继续修改的模型与 H2S 切片工程。用户不必学习建模或了解 AMS。最终由用户在 Bambu Studio 中打开、精修并决定是否打印。本技能不连接打印机、不读取打印机凭据、不发送打印、不改变设备配置；另行要求设备操作时作为独立任务处理。默认本地处理；用户明确要求使用 Tripo 等云端生成时，可在已授权范围上传指定参考图或模型、使用其 API 额度，按 [Tripo 云端建模](references/tripo.md) 执行。不得将参考图、模型或密钥随技能发布到公共仓库。

不以在线打印机、AMS 或访问码为前置条件。本地软件预设不等于实际硬件状态。可只读使用已知的 Studio 打印预设，避免读取同目录的账户/令牌文件。

## 需求与设计

中文沟通，统一毫米。提取用途、外形、尺寸、关键孔距/间隙、颜色、装配和承重要求。只询问会改变设计的缺失信息；可逆外观选择先用合理默认。缺少实测尺寸不能编造精确配合。

未知硬件时标注设计假设：H2S、0.4 mm 喷嘴、PLA、0.20 mm 层高。需要切片时从可用预设选择打印板并注明。H2S 标称空间 340×320×340 mm；支撑、裙边、擦料塔和禁区仍以实际配置切片为准。假设不能表述为读取到的机器状态。

尺寸驱动的盒子、支架、外壳与装配件优先 CadQuery；已有可靠 OpenSCAD 工作流也可沿用。用户选择 Tripo 图生 3D 时，默认使用 Tripo API → 轻量 Python 网格/颜色处理 → Bambu Studio CLI，不要求 Blender。Blender 仅作为复杂雕刻、结构修改或用户已有工作流的可选后端。保留原始网格、处理参数与可编辑工程。

初次运行或工具失效读取 [本地环境](references/environment.md)。建模前读取 [建模与检查](references/modeling.md)。

照片人物、相似度修订或已知料槽数量时，读取 [人物与颜色预算](references/portraits-and-palettes.md)。先验证头像近照与颜色映射，再投入完整切片。

照片人像若需要高相似度，优先使用用户授权的图生 3D 或其现有优质网格作为起点；不要反复用通用参数化五官代替照片人物。云端生成负责形状与贴图，本地负责尺寸、底座、封闭性、颜色槽位和切片。API 成功、照片般贴图和可打印几何是三个不同的验收项。

## 实施闭环

1. 写 `design-spec.json`：用途、毫米单位、尺寸、参数、部件/颜色表、假设和待确认项。后续修改优先更新参数，避免仅缩放最终网格。
2. 用可执行源文件生成真实几何。预览和导出采用同一参数，不以概念图代替模型预览。
3. 生成整体预览及必要的正交视图，装配增加爆炸或剖视图。实际打开图片检查外形、孔位、文字、悬空与接触面，修复后重生成。
4. 检查每个实际打印部件，按 [H2S 工程与切片验证](references/bambu-project.md) 创建工程。导出成功不代表封闭、壁厚合适或装配可用。
5. 完成真实切片验证，保留可编辑工程与独立切片结果。检查空层、丢失薄壁、支撑、超限与耗材分配，说明不能执行的检查。
6. 交付文件和验证级别。修改后更新受影响的预览、工程与检查，不混用旧图和新模型。

## 颜色与材料

AMS 是自动切换多卷耗材的供料系统，本阶段无需配置。H2S 为单喷嘴，多色表达为可分配耗材的部件/区域。逻辑耗材槽不等于实机 AMS 槽，不能声称已匹配。

Blender 材质、贴图、透明或金属效果不能直接等同于打印颜色。STL 不保留颜色。记录部件到颜色及逻辑耗材的映射，并验证工程确实保留它们。少换色和分件组装是可选优化，不擅自改变外观；未知 AMS 时说明单色/分件方式。

## 交付与验收

使用用户指定目录，否则交付到当前任务 `outputs/<model-name>/`，中间文件放 `work/`。至少包括：
- 可修改源文件与参数说明；CAD 可附 STEP，Blender 保留 `.blend` 和生成脚本。
- 每个打印部件的 STL 或保留部件的通用 3MF。
- 同一几何的 PNG 预览，必要时加离线交互预览。
- H2S Bambu Studio 工程 `.3mf`；真实切片成功才能称为已验证可切片。
- 中文使用说明及 `validation.json`：工具版本、毫米尺寸、几何结果、配置来源、假设、切片结果、实际切片器提供的时间/耗材估算及未解决项。

四级状态：`geometry_only` 仅模型；`project_created` 已有工程但未实际切片；`slice_verified` H2S 配置真实切片成功；`studio_reviewed` 另已打开 Studio 检查。ZIP/XML 检查、渲染或 CLI 退出码均不能独立代表完整切片验证。

若环境无法生成原生工程，继续交付源、预览和通用模型，清楚标注 H2S 工程验收未完成及最小阻塞。不得改后缀、制造空壳工程，或把通用 3MF 描述为完整 H2S 工程。最终说明文件、假设、验证结果与 Studio 打开方式，允许用户继续以自然语言迭代。
